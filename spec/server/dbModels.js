const expect = require('chai').expect;

describe('sockets', () => {

  beforeEach((done) => {
  console.log('beforeEach ' , beforeEach);
    done();
  });

  it('should be a socket test', (done) => {
    
    done();
  });

});